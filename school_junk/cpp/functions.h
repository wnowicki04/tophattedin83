#ifndef H_FUNCTIONS
#define H_FUNCTIONS //definition macro

//projects of functions, defined in "functions.cpp"

void calc(float& a, float& b);

void preswap(int& x, int& y);
void swap(int& x, int& y);
void postswap(int& x, int& y);

#endif //ending a variable directive event